Read Linear issue $ARGUMENTS from our workspace.

The issue should already have a detailed specification. If it doesn't, ask me whether I want you to write a spec first.

Implement the solution following the spec:
1. Create a feature branch named after the issue (e.g., `feature/CCM-37-description`)
2. Move the issue to "In Progress" in Linear
3. Implement the changes following our codebase patterns
4. Add progress comments to the Linear issue for significant milestones
5. When done, move the issue to "Done"
6. Add a summary comment on the issue describing what was implemented

If you discover additional work needed, create new Linear issues with:
- Appropriate labels (Feature, Improvement, Bug, or custom: frontend, backend, payment, etc.)
- Project: Varro - Frontend or Varro - Backend
- State: Backlog

Do not expand the current issue's scope.
