# [P3] `ncLogoFooter.vue` and `ncLogoHeader.vue` removed text portion of SVG logo

## File
components/ncLogoFooter.vue, components/ncLogoHeader.vue

## Issue
The SVGs were changed from wider viewBoxes (279x24, 324x28) to narrower ones (181x24, 210x28) and the text paths (the "NEW COMMONS CHALLENGE" text rendered in SVG) were removed, leaving only the icon grid. If the branding has officially changed from "New Commons Challenge" to just the icon, this is correct. But the commit should note this is an intentional brand change, not a bug.

## Suggested Fix
Verify with the client/designer that the text removal from the logo SVGs is intentional. If the new brand is "New Commons" (without "Challenge"), consider adding an `aria-label` to the SVGs for accessibility since the text is no longer visible.

## Status
pending
