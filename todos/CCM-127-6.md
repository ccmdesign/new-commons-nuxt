# [P2] ncTimeline component refactored but unused -- dead code in PR

## File
components/ncTimeline.vue

## Issue
The ncTimeline component was significantly refactored (props added, hardcoded data removed, date formatting extracted, active/full state logic added). However, a search for `nc-timeline` or `ncTimeline` across all pages and components shows it is not used anywhere. The previous usage in `index.vue` was commented out (`<!--<nc-timeline id="timeline" />-->`) and that comment was removed in this PR. This means the component refactoring is dead code.

## Suggested Fix
Either remove the component entirely (it can be re-added when needed), or add a TODO comment at the top of the file noting it is currently unused and pending future integration.

## Status
resolved
