# [P2] ncTopbar.vue has empty script setup block after removing useSiteLinks

## File
components/ncTopbar.vue

## Issue
The PR removed `const { rulesUrl } = useSiteLinks()` from the `<script setup>` block (along with the Rules nav item) but left the block completely empty with just a blank line. While this is valid Vue 3, it is unnecessary boilerplate. The `useSiteLinks` composable is still imported and used elsewhere (`the-2025-challenge.vue`), so it is not orphaned, but the empty script block in ncTopbar is dead code.

## Suggested Fix
Remove the empty `<script setup>` block entirely from `ncTopbar.vue`.

## Status
pending
